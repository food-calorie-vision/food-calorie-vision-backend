# 🎯 NRF9.3 영양 점수 시스템 구현 가이드

## 📊 NRF9.3 (Nutrient Rich Food Index) 개요

### **공식**
```
NRF9.3 = (∑ 권장영양소 % / 9) - (∑ 제한영양소 % / 3)
```

### **권장영양소 9가지**
1. 단백질 (Protein) - 55g
2. 식이섬유 (Fiber) - 25g
3. 비타민 A (Vitamin A) - 700μg RAE
4. 비타민 C (Vitamin C) - 100mg
5. 비타민 E (Vitamin E) - 12mg
6. 칼슘 (Calcium) - 700mg
7. 철분 (Iron) - 10mg
8. 칼륨 (Potassium) - 3500mg
9. 마그네슘 (Magnesium) - 350mg

### **제한영양소 3가지**
1. 포화지방 (Saturated Fat) - 15g
2. 첨가당 (Added Sugar) - 50g
3. 나트륨 (Sodium) - 2000mg

---

## 🔄 전체 시스템 플로우

```
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: 식재료 입력 (/meal-diary/ingredient)                │
│  ├─ 이미지 업로드                                            │
│  ├─ Roboflow + GPT Vision 분석                               │
│  └─ UserIngredient 테이블 저장 (is_used = False)             │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  STEP 2: 음식 추천 받기                                      │
│  ├─ GET /api/v1/ingredients/recommendations                  │
│  ├─ 보유 식재료 + 사용자 건강정보 기반                        │
│  ├─ GPT-4o-mini가 추천 음식 3-5개 반환                       │
│  └─ 각 음식마다 필요한 식재료 목록 포함                       │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  STEP 3: 추천 음식 선택 및 저장 ⭐                           │
│  POST /api/v1/meals/save-recommended                         │
│                                                               │
│  3-1. 식재료 사용 처리                                       │
│      ├─ UserIngredient에서 해당 식재료 조회                  │
│      ├─ count > 1: count -= 1 (수량 감소)                    │
│      └─ count == 1: is_used = True (사용 완료)               │
│                                                               │
│  3-2. GPT로 영양소 추론                                      │
│      ├─ 음식명 + 섭취량 → GPT-4o-mini                        │
│      └─ 칼로리 + 12가지 영양소 JSON 반환                     │
│                                                               │
│  3-3. NRF9.3 점수 계산                                       │
│      ├─ 권장영양소 9가지 % 계산 → 평균                       │
│      ├─ 제한영양소 3가지 % 계산 → 평균                       │
│      └─ final_score = positive - negative                    │
│                                                               │
│  3-4. DB 저장                                                │
│      ├─ Food 테이블 확인/생성                                │
│      ├─ UserFoodHistory 저장                                 │
│      └─ HealthScore 저장 (NRF9.3 점수)                       │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  STEP 4: 대시보드 표시 (/dashboard)                          │
│  GET /api/v1/meals/dashboard-stats                           │
│  ├─ 평균 NRF9.3 점수                                         │
│  ├─ 오늘 총 칼로리                                           │
│  ├─ 자주 먹는 음식 Top 5                                     │
│  └─ 일일 칼로리 차트 (최근 7일)                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ API 엔드포인트

### **1. 음식 추천 받기**
```http
GET /api/v1/ingredients/recommendations
```

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "recommendations": "⚠️ 본 추천은 AI 기반 일반적인 조언이며...\n\n🍽️ 1. 김치찌개\n🥕 필요한 재료: 김치, 두부, 대파...",
    "ingredients_used": ["양파", "감자", "당근"],
    "total_ingredients": 5
  }
}
```

### **2. 추천 음식 선택 및 저장** ⭐
```http
POST /api/v1/meals/save-recommended
```

**요청 Body:**
```json
{
  "food_name": "김치찌개",
  "ingredients_used": ["김치", "두부", "대파"],
  "meal_type": "점심",
  "portion_size_g": 300.0,
  "memo": "오늘 점심으로 김치찌개 먹음"
}
```

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "history_id": 123,
    "food_name": "김치찌개",
    "calories": 450,
    "health_score": 15.5,
    "food_grade": "보통 영양식품"
  },
  "message": "✅ 김치찌개 기록 완료! NRF9.3 점수: 15.5점"
}
```

### **3. 음식 직접 기록 (사진 촬영)**
```http
POST /api/v1/meals/save
```

**요청 Body:**
```json
{
  "meal_type": "점심",
  "foods": [{
    "food_id": "food_001",
    "food_name": "김치찌개",
    "portion_size_g": 300,
    "calories": 450,
    "protein": 20,
    "carbs": 40,
    "fat": 15,
    "sodium": 1200
  }]
}
```

### **4. 대시보드 통계**
```http
GET /api/v1/meals/dashboard-stats
```

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "total_calories_today": 1800,
    "total_calories_week": 12600,
    "avg_health_score": 45.5,
    "frequent_foods": [
      {"food_name": "김치찌개", "count": 5},
      {"food_name": "제육볶음", "count": 3}
    ],
    "daily_calories": [],
    "nutrition_balance": {}
  }
}
```

---

## 💾 데이터베이스 구조

### **UserIngredient** (식재료)
```sql
ingredient_id (PK, AUTO_INCREMENT)
user_id (FK → User)
ingredient_name (varchar)
count (int) -- 수량
created_at (datetime)
is_used (boolean) -- 사용 여부
```

### **Food** (음식)
```sql
food_id (PK, varchar)
food_name (varchar)
category (varchar)
food_class_2 (varchar)
```

### **UserFoodHistory** (음식 섭취 기록)
```sql
history_id (PK, AUTO_INCREMENT)
user_id (FK → User)
food_id (FK → Food)
food_name (varchar)
consumed_at (datetime) -- 먹은 시각
portion_size_g (decimal) -- 섭취량
Field (varchar) -- 메모
```

### **HealthScore** (건강 점수)
```sql
history_id (PK, FK → UserFoodHistory)
user_id (FK → User)
food_id (FK → Food)
kcal (int) -- 칼로리
positive_score (int) -- 권장영양소 점수
negative_score (int) -- 제한영양소 점수
final_score (int) -- NRF9.3 최종 점수
food_grade (varchar) -- 영양 등급
calc_method (varchar) -- 계산 방식
```

---

## 🧮 NRF9.3 계산 예시

### **사과 100g**

**영양소:**
- 단백질: 0.3g → (0.3/55) × 100 = 0.55%
- 식이섬유: 2.4g → (2.4/25) × 100 = 9.6%
- 비타민C: 4.6mg → (4.6/100) × 100 = 4.6%
- 칼륨: 107mg → (107/3500) × 100 = 3.06%
- 첨가당: 10.4g → (10.4/50) × 100 = 20.8%

**계산:**
```
positive_score = (0.55 + 9.6 + 4.6 + 3.06 + ... ) / 9 = 약 5.2
negative_score = (20.8 + 0 + 0) / 3 = 약 6.9
raw_score = 5.2 - 6.9 = -1.7

# 0~100 범위로 정규화
final_score = (-1.7 + 100) / 2 = 49.15점
```

**등급:** 보통 영양식품 (50-74점)

---

## 🎯 점수 등급 기준

| 점수 | 등급 | 설명 |
|------|------|------|
| 90점 이상 | 우수한 영양식품 | 매우 건강한 식품 |
| 75-89점 | 좋은 영양식품 | 건강한 식품 |
| 50-74점 | 보통 영양식품 | 적당한 영양가 |
| 25-49점 | 영양개선 필요 | 개선이 필요함 |
| 24점 이하 | 영양소 부족 | 영양가 낮음 |

---

## 📊 실제 음식 점수 예시 (0~100점 범위)

### **🍎 사과 100g**
```
positive_score: 5.2 (권장영양소 평균)
negative_score: 6.9 (제한영양소 평균 - 당분 높음)
raw_score: -1.7
final_score: 49.15점 ✨
등급: "보통 영양식품"
```

### **🐟 연어 덮밥 300g**
```
positive_score: 65.3 (단백질, 비타민D, 오메가3 풍부)
negative_score: 25.8 (나트륨 약간 높음)
raw_score: 39.5
final_score: 69.75점 ✨
등급: "보통 영양식품"
```

### **🥗 채소 샐러드 200g**
```
positive_score: 78.5 (비타민, 섬유질, 미네랄 풍부)
negative_score: 8.2 (제한영양소 매우 낮음)
raw_score: 70.3
final_score: 85.15점 ✨
등급: "좋은 영양식품"
```

### **🍲 김치찌개 300g**
```
positive_score: 35.0 (단백질, 섬유질 보통)
negative_score: 55.0 (나트륨 매우 높음)
raw_score: -20.0
final_score: 40.0점 ✨
등급: "영양개선 필요"
```

### **🍗 닭가슴살 샐러드 250g**
```
positive_score: 85.0 (단백질, 비타민, 미네랄 매우 풍부)
negative_score: 12.0 (제한영양소 낮음)
raw_score: 73.0
final_score: 86.5점 ✨
등급: "좋은 영양식품"
```

### **🍕 페퍼로니 피자 200g**
```
positive_score: 25.0 (영양소 부족)
negative_score: 75.0 (포화지방, 나트륨, 당분 매우 높음)
raw_score: -50.0
final_score: 25.0점 ✨
등급: "영양개선 필요"
```

---

## 📱 프론트엔드 통합

### **추천 음식 선택 시**

```typescript
// 추천 받기
const response = await fetch('/api/v1/ingredients/recommendations');
const recommendations = await response.json();

// 사용자가 "김치찌개" 선택
const saveResponse = await fetch('/api/v1/meals/save-recommended', {
  method: 'POST',
  body: JSON.stringify({
    food_name: "김치찌개",
    ingredients_used: ["김치", "두부", "대파"],
    meal_type: "점심",
    portion_size_g: 300
  })
});

const result = await saveResponse.json();
alert(`✅ 저장 완료! NRF9.3 점수: ${result.data.health_score}점`);
```

---

## ⚙️ 환경 변수

```env
# .env 파일
OPENAI_API_KEY=your_openai_api_key
DATABASE_URL=mysql+aiomysql://user:pass@localhost/db_name
```

---

## 🧪 테스트 방법

### **1. 식재료 입력 → 음식 추천**
```bash
# 식재료 저장
curl -X POST http://localhost:8000/api/v1/ingredients/save \
  -H "Content-Type: application/json" \
  -d '{
    "ingredients": [
      {"name": "김치", "count": 1},
      {"name": "두부", "count": 1},
      {"name": "대파", "count": 1}
    ]
  }'

# 음식 추천 받기
curl http://localhost:8000/api/v1/ingredients/recommendations
```

### **2. 추천 음식 선택 및 저장**
```bash
curl -X POST http://localhost:8000/api/v1/meals/save-recommended \
  -H "Content-Type: application/json" \
  -d '{
    "food_name": "김치찌개",
    "ingredients_used": ["김치", "두부", "대파"],
    "meal_type": "점심",
    "portion_size_g": 300
  }'
```

### **3. 대시보드 통계 조회**
```bash
curl http://localhost:8000/api/v1/meals/dashboard-stats
```

---

## 🚀 향후 개선 사항

1. **GPT 영양소 추론 정확도 향상**
   - food_nutrients DB에서 유사 음식 검색
   - 검색 결과를 GPT에 Few-shot으로 제공
   
2. **사용자별 일일 권장량 커스터마이징**
   - 나이, 성별, 체중, 활동량 반영
   - User 테이블의 정보 활용

3. **실시간 영양소 추적**
   - 오늘 섭취한 영양소 누적 계산
   - 일일 권장량 대비 % 표시

4. **주간/월간 건강 리포트**
   - HealthReport 테이블 활용
   - GPT로 맞춤형 건강 조언 생성

---

## 📞 문의

문제가 발생하거나 개선 제안이 있으시면 이슈를 등록해주세요!

